name: "Terrain Follower"
description: "Terrain following pomoci downward lidar (standalone, 1 dron). Vyzaduje bezici lidar_bridge scenar."
ros2_command: "ros2 run scout_control terrain_follower --ros-args -p desired_height:=3.0"
